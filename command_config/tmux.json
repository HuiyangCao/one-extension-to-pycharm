{
  "commands": [
    {
      "name": "tmux attach",
      "command": "tmux attach -t {session_name}",
      "parameters": {
        "session_name": {
          "type": "string",
          "prompt": "请输入 tmux session 名称或编号"
        }
      }
    }
  ]
}
